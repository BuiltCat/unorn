---
title: Flex Direction
---

import { Card, CardGrid } from '@astrojs/starlight/components';

`flex-direction`属性决定主轴的方向（即项目的排列方向），`React Native`的默认主轴方向为`column`。

## 基本使用

使用**flex-row** | **flex-row-reverse** | **flex-col** | **flex-col-reverse** 设置元素的flexDirection。

```typescript
'flex-row' -> { flexDirection: 'row' }
'flex-row-reverse' -> { flexDirection: 'row-reverse' }
'flex-col' -> { flexDirection: 'column' }
'flex-col-reverse' -> { flexDirection: 'column-reverse' }
```

## 文档

如果对此属性不熟悉，可以查看React Native的官方文档，链接在这里。

[Layout Props#flexdirection](https://reactnative.dev/docs/layout-props#flexdirection)